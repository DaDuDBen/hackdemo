# Frontend (React + Vite)

## Features

- Invoice creation form
- Invoice list
- Invoice detail view
- Timeline display
- Escalation badge

## Run

```bash
npm install
npm run dev
```

Set API base URL in root `.env` as `VITE_API_BASE_URL` if needed.
